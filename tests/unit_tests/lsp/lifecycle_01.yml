# RUN: %cxx -lsp-test < %s | %filecheck %s

{ "method": "initialize", "id": 0 }

# CHECK: "result":
# CHECK: "capabilities":
# CHECK: "textDocumentSync":

# CHECK: "serverInfo":
# CHECK: "name":
# CHECK: "version":

{ "method": "shutdown", "id": 1 }

{ "method": "exit" }



